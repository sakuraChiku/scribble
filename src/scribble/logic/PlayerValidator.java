package scribble.logic;

import scribble.models.Move;
import scribble.models.Player;

public class PlayerValidator {
    public boolean canMakeWord(Move move, Player player) {
        return true;
    }
}
